---
apiVersion: 1
datasources:
  - name: prometheus
    # <string, required> datasource type. Required
    type: prometheus
    # <string, required> access mode. proxy or direct (Server or Browser in the UI). Required
    access: proxy
    # <int> org id. will default to orgId 1 if not specified
    orgId: 1
    # <string> url
    url: http://keisuke-jpn-1:9090/prometheus
    # <string> Deprecated, use secureJsonData.password
    password:
    # <string> database user, if used
    user:
    # <string> database name, if used
    database:
    # <bool> mark as default datasource. Max one per org
    isDefault: true
    version: 1
    editable: false

  - name: InfluxDB
    type: influxdb
    access: proxy
    orgId: 1
    url: http://keisuke-jpn-1:8086
    database: latency
    version: 1
    editable: false

  - name: InfluxDB-telegraf
    type: influxdb
    access: proxy
    orgId: 1
    url: http://keisuke-jpn-1:8086
    database: telegraf
    version: 1
    editable: false