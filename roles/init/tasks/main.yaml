---
  - name: Update APT cache
    ansible.builtin.apt:
      cache_valid_time: 86400

  - name: sudo name resolve
    ansible.builtin.lineinfile:
      path: /etc/hosts
      line: '127.0.0.1 {{ inventory_hostname }}'
      owner: root
      group: root 
      mode: '0644'